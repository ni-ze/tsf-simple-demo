server:
  port: 18084
spring:
  application:
    name: cmq-producer-demo
  cloud:
    stream:
      bindings:
        input:
          destination: test-topic
          group: queue
        output:
          destination: test-topic
      cmq:
        binder:
          secretId:
          secretKey:
          endpoint: 